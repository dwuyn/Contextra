services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-contextra}
      POSTGRES_USER: ${POSTGRES_USER:-contextra}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-contextra}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 5s
    restart: unless-stopped

  migrate:
    image: ${MIGRATE_IMAGE:-ghcr.io/dwuyn/contextra-migrate:latest}
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-contextra}:${POSTGRES_PASSWORD:-contextra}@postgres:5432/${POSTGRES_DB:-contextra}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  next-app:
    image: ${APP_IMAGE:-ghcr.io/dwuyn/contextra-app:latest}
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-contextra}:${POSTGRES_PASSWORD:-contextra}@postgres:5432/${POSTGRES_DB:-contextra}?schema=public
      HOSTNAME: 0.0.0.0
      NODE_ENV: production
      PORT: 3000
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - avatar-data:/app/data
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

volumes:
  postgres-data:
  avatar-data:
